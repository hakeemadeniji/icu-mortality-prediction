# Frontend configuration (template). Copy to `.env.local`.
# These are public (NEXT_PUBLIC_*) and safe to expose — no secrets here.
NEXT_PUBLIC_API_URL=http://localhost:8054
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3054
